{
  "name": "loom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun scripts/dev.mjs",
    "dev:web": "bun scripts/dev.mjs",
    "dev:api": "bun --filter @loom/server dev",
    "start": "bun --filter @loom/server start",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "bun run --workspaces build",
    "build:web": "bun --filter @loom/web build",
    "format": "prettier --write 'packages/**/*.{ts,tsx,css,json}' '*.{json,md}' --ignore-path .gitignore",
    "format:check": "prettier --check 'packages/**/*.{ts,tsx,css,json}' '*.{json,md}' --ignore-path .gitignore",
    "prepare": "husky"
  },
  "workspaces": [
    "packages/*"
  ],
  "trustedDependencies": [
    "esbuild"
  ],
  "lint-staged": {
    "*.{ts,tsx,css,json,md}": "prettier --check --ignore-path .gitignore"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "prettier": "^3.9.4",
    "prettier-plugin-tailwindcss": "^0.8.0",
    "typescript": "^5.9.0",
    "vitest": "^2.1.0"
  }
}
